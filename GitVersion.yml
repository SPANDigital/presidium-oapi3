mode: ContinuousDelivery
next-version: 2.11.0
branches:
  develop:
    mode: ContinuousDeployment
    tag: rfv
    increment: Patch
    regex: (^develop$|^origin\/develop$)
    source-branches: []
  master:
    mode: ContinuousDelivery
    regex: (^main$|^origin\/main$)
    source-branches: []
ignore:
  sha: []
